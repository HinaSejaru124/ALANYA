package controllers;

/**
 * Classe utilitaire pour améliorer l'accessibilité de l'interface
 */
public class StyleUpdater {
    
    /**
     * Augmente la taille de police des éléments d'interface
     * @param factor facteur d'augmentation (ex: 1.2 pour 20% plus grand)
     */
    public void increaseFontSize(double factor) {
        // Cette méthode serait implémentée pour modifier les styles
        // des éléments d'interface utilisateur
        
        // Note: dans une implémentation réelle, on parcourrait
        // tous les éléments UI et on appliquerait le facteur d'échelle
    }
    
    /**
     * Augmente le contraste des éléments pour une meilleure lisibilité
     */
    public void increaseContrast() {
        // Implémentation qui améliorerait le contraste des éléments
    }
    
    /**
     * Active ou désactive le mode d'accessibilité élevée
     * @param enabled true pour activer, false pour désactiver
     */
    public void setHighAccessibilityMode(boolean enabled) {
        // Implémentation qui ajusterait de nombreux paramètres
        // pour une meilleure accessibilité
    }
}